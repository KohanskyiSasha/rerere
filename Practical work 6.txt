#include <windows.h>
#include <iostream>
#include <conio.h>
#include <iomanip>
using namespace std;

int main()
{
	SetConsoleCP(1251);
	SetConsoleOutputCP(1251);
	//Завдання 1
	
	double a, b, c, x, F;
cout << "Input a, b, c, x:";
cin >> a >> b >> c >> x;
if (x == 0 && b != 0) {
	F = a * pow((x + c), 2) - b/3;
}
else
	if (x != 0 && b == 0 && c != 0) {
		F = a/(-c) + pow(b, 2) + 3;
	}
	else 
	if( c != 0){
		F = a + x / c - 7;
	}
cout  << "F =" << setprecision(3) << F;
}
